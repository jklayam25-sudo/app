package lumi.insert.app.entity;

public enum TransactionSortOrder {
    createdAt,
    updatedAt,
    totalItems,
    totalFee,
    totalDiscount,
    subTotal,
    grandTotal,
    totalUnpaid,
    totalPaid,
    totalUnrefunded,
    totalRefunded;
}
